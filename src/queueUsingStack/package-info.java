package queueUsingStack;

//Queue is implemented using Stack
//Push Operation is same in both
//But, pop operation in queue remove the first inserted element, whereas in the stack it removes the last inserted one